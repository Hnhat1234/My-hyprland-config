# Taste (Continuously Learned by [CommandCode][cmd])

[cmd]: https://commandcode.ai/

# gtk
- Use string-based anchors like `anchor="top"` for window positioning in AGS — not `Gtk.Anchor.TOP` or bare enum `TOP`. Confidence: 0.70
